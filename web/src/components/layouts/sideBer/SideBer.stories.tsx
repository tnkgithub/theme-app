import { Meta, StoryObj } from '@storybook/react';
import SideBer from './SideBer';

const meta: Meta = {
  title: 'Layouts/SideBer',
  component: SideBer,
};

export default meta;

export const Default: StoryObj = {
  args: {},
};
